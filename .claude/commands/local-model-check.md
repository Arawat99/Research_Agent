---
description: Check whether Ollama is running and confirm the active model list.
---

Run the local Ollama health check and summarize the result.

Use the project helper:

```bash
python3 scripts/ollama_status.py
```

Then explain whether the model is ready for use, which model is installed, and any next steps if it is not.
